# Deployment

This directory is owned by the root parent project and will contain environment-specific deployment assets for the platform.

- `docker-compose/`: local integration and developer environments
- `kubernetes/`: runtime deployment descriptors and environment overlays
