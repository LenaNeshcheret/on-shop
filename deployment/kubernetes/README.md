# Kubernetes

This directory will contain Kubernetes deployment assets for the platform:

- service manifests or Helm charts
- environment overlays
- configuration, secrets references, and operational policies

Kubernetes is the target runtime environment for deployed services.
