# Docker Compose

This directory will contain the local integration environment:

- infrastructure services such as PostgreSQL, MongoDB, Elasticsearch, and Kafka
- Java services built from Gradle modules
- the Go pricing service

Compose is for local development and integration only, not production.
